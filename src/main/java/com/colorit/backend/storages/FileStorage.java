package com.colorit.backend.storages;

import org.springframework.core.env.Environment;

public class FileStorage implements IStorage {
    IStorage storage;

    FileStorage() {
        Environment env;
        if (env.getActiveProfiles())
    }
    @Override
    public
}
